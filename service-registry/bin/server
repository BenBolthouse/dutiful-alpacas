#!/usr/bin/env node

const service = require("../index");
const config = require("../config");
const log = service.get("log");

const listener = service.listen(config.port || 0, () => {
  log.info(`Listening on ${config.schema}://${config.url}:${listener.address().port}`);
});
